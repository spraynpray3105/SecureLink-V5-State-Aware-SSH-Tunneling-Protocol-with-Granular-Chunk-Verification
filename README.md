# SecureLink-V5-State-Aware-SSH-Tunneling-Protocol-with-Granular-Chunk-Verification
A custom Python 3.13 protocol that utilizes Ed25519 SSH tunnels to transmit data through a segmented hashing system. Features a unique 'Challenge-Response' architecture where the receiver must query the sender to verify encrypted chunks before reconstructing the final message.
